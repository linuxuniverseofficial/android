# Install from F-Droid or GitHub!

pkg install openssh
sshd
passwd
whoami
ip a

ssh u0_a227@172.20.0.109 -p 8022

# pkg upgrade
Mark YES for all changes

# pkg install root-repo x11-repo proot-distro sudo wget git openssh busybox htop net-tools iperf3 whois traceroute inxi curl nmap lz4 cmatrix yq iproute2 termux-api sshpass

curl -sSL https://git.linuxuniverse.com.br/bashrc/bashrc | tee /data/data/com.termux/files/usr/etc/bash.bashrc
Add to:
nano /data/data/com.termux/files/usr/etc/bash.bashrc
[ -r /data/data/com.termux/files/usr/share/bash-completion/bash_completion ] && . /data/data/com.termux/files/usr/share/bash-completion/bash_completion

EASY MENUS CONFIG!
